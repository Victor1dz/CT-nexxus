server:
  port: 8080

spring:
  application:
    name: ct-nexxus

  datasource:
    url: jdbc:h2:mem:ctnexxusdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  thymeleaf:
    cache: false

  # ====== EMAIL (SMTP) ======
  mail:
    host: smtp.gmail.com
    port: 587
    username: victorero348@gmail.com
    password: yzhv fnnx sjlb xlrx
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Configs do sistema
ctnexxus:
  lembrete:
    dias-antes: 2,1,0
    hora-envio: "09:00"
